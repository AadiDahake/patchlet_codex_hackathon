node_modules/
.next/
dist/
out/
.env
.env.*
!.env.example
.vercel/
*.log
.DS_Store
__pycache__/
.venv/
*.pyc
.pytest_cache/
.ruff_cache/
*.tsbuildinfo
# Next writes this file on every dev and build run, and writes a different path
# into it for each, so a committed copy can never stay clean.
apps/web/next-env.d.ts
# apps/web/public/widget.js is committed so the dashboard deployment serves it
# Local guidance for a Claude session; AGENTS.md is the shared file.
CLAUDE.md
